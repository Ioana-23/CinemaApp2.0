spring:
  application:
    name: movie_screening_service
  datasource:
    password: postgres
    username: postgres
    url: jdbc:postgresql://localhost:5434/MovieScreeningServiceDatabase
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
server:
  port: 8082
springdoc:
  paths-to-match: /project/**