spring:
  application:
    name: movie_service
  datasource:
    password: postgres
    username: postgres
    url: jdbc:postgresql://localhost:5435/MovieServiceDatabase
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
server:
  port: 8083
springdoc:
  paths-to-match: /project/**