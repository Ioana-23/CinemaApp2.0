spring:
  application:
    name: user_service
  datasource:
    password: postgres
    username: postgres
    url: jdbc:postgresql://localhost:5433/UserServiceDatabase
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
server:
  port: 8081
springdoc:
  paths-to-match: /project/**