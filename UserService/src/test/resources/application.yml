spring:
  flyway:
    enabled: false
  datasource:
    password:
    username: test
    url: jdbc:h2:mem:UserServiceTestDatabase
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
server:
  port: 8081